#!/usr/bin/env bash

set -o errexit
set -o nounset
set -o pipefail

export GOBIN="${ASDF_INSTALL_PATH}/bin"
go install k8s.io/code-generator/cmd/{client,conversion,defaulter,informer,lister,openapi}-gen@v"${ASDF_INSTALL_VERSION}"
